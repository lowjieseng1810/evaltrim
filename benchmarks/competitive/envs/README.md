# Isolated competitor environments (not installed into EvalTrim's interpreter).
#
# Runtime trees live under /tmp/evaltrim-comp/ (gitignored).
# Lockfile / hashes recorded in environment.yaml after each measured run.

Do not `pip install` competitors into the EvalTrim editable environment.
